# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_from_botfather

# Linear Integration (Required for agent creation)
LINEAR_API_KEY=your_linear_api_key

# GitHub Integration (Required for repository access)
GITHUB_TOKEN=your_github_personal_access_token

# Environment
NODE_ENV=production
PORT=3000

# Database Configuration
DATABASE_PATH=./data/agents.db

# Docker Configuration
DOCKER_WORKSPACE_PATH=./workspace
MAX_DOCKER_INSTANCES=10
DOCKER_MOCK_MODE=false

# Claude Configuration (CLI authentication handled separately)
CLAUDE_USE_API=false

# Logging
LOG_LEVEL=info